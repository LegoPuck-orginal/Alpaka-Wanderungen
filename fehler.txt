Protokoll (4. Oktober 2025)

1. `php artisan migrate:fresh --seed`
   → Alle Tabellen neu angelegt (SQLite) und Seed-Daten eingespielt.

2. `php artisan test`
   → 27 Tests, 65 Assertions – alle erfolgreich.

3. `composer dev`
   → Hinweis: Startet lokalen Server, Queue-Listener, Log-Tail und Vite parallel (optional im Alltag verwenden).
